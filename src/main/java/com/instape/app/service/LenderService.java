package com.instape.app.service;

import org.springframework.data.domain.Pageable;
import com.instape.app.cloudsql.model.LenderDTO;
import com.instape.app.response.PageableResponseDTO;

public interface LenderService {

//	PageableResponseDTO getAll(Pageable pageable);
//
//	LenderDTO getById(Long lenderId);
//
//	LenderDTO create(Long loggedInUser, LenderDTO lender);
//
//	LenderDTO update(Long loggedInUser, LenderDTO lender);
//
//	void delete(Long loggedInUser, Long lenderId);
//
//	boolean isExistWithSameCode(String lenderCode);

}
